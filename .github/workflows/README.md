# deploy
For the deploy.yml to work, you’ll need to add this secret under
` Settings → Secrets and variables → Actions → New repository secret:`

```nginx
PYPI_API_TOKEN
```

> (Get it from your PyPI account → Account settings → API tokens)
