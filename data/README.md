# Add the data for the model here
# Don't store the original data files in the repo, just the processed data
# It's recommended to pubish data on a data hosting service like Zenodo or Kaggle, otherwise use git-lfs for large files.
